<div class="dashboard-body">
  <div class="row">

    <div class="col-3">
      <%= render "shared/left_navbar"%>
    </div>

    <div class="col-9">
      <div class="container mt-3">

        <h1>Mis grupos en los que estoy</h1>
          <% current_user.group_invitations.each do |group| %>
            <% if group.status.eql?(true) %>
            <div class="card p-5 my-2">


              <p><%= group.group.name %> (Invitado por: <span><%= group.group.user.username %></span>)</p>
              <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
          Ver contraseñas
        </button>
        <% group.group.passwords.each do |x| %>

          <%= x.site_name %>
        <% end %>


             <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
          <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
            <div class="modal-content">
              <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Mis contraseñas</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                <p><% group.group.passwords.each do |password| %></p>
                  <div class="card">
                    <p>Sitio: <%= password.site_name %></p>
                    <label for="oculto">Password:</label>
                    <input id="oculto" type="text" readonly value='<%= password.site_password %>'>
                  </div>
                <% end %>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerar</button>
                <button type="button" class="btn btn-primary">Agregar</button>
              </div>
            </div>
          </div>
        </div>
            </div>
            <% end %>

              <% end %>
      </div>
    </div>
  </div>
</div>
